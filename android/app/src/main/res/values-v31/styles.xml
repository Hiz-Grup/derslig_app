<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12 (API 31) ve üzeri için edge-to-edge desteği -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Splash ekranı için arka plan -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Edge-to-edge desteği -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
    
    <!-- Normal tema - Android 12+ için özelleştirilmiş -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <!-- Edge-to-edge desteği için gerekli ayarlar -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <!-- Transparent navigation ve status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>

